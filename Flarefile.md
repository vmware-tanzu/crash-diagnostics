FROM        //  <machine name> (default local)>
AS          // <gid/uid>, default current group/user
WORKDIR     // working directory
COPY        // <File or directory list> 
CAPTURE     // command output
